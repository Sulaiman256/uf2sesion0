{
  "name": "uf2sesion0",
  "version": "1.0.0",
  "description": "1. Hemos creado un proyecto con vscode\r 2. Hemos abierto la terminal con ctrl + mayus + ñ\r 3. Hemos iniciado el proyecto (y por lo tanto creado el package.json)\r 4. Hemos instalado una libreria que se lama 'lite-server' con npm install lite-server y se nos ha creado la carpeta node modules (Esta libreria nos permite levantar un servidor local localhost;puerto)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lite-server": "^2.6.1"
  }
}
